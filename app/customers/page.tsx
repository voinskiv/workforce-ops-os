import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function CustomersPage() {
  return (
    <PagePlaceholder
      title="Kunden"
      description="Zeigt Kunden und Standorte als operative Struktur. Wichtig, weil Status, Einsätze, Zeitprüfung und Vorgänge immer mit Kunde und Standort verbunden sind."
    />
  );
}
